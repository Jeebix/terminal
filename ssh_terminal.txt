// connexion serveur en ssh
ssh -A web@jbb-php7.dmp

// pour connexion ssh et mémorisation clé
eval $(ssh-agent -s)
ssh-add -K

// ajout clé au trousseau Mac
ssh-add -K

// changer les droits
chown web:www-data -R csv/
